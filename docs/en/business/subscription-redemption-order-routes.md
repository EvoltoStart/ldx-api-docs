# Subscription, Redemption Code, and Order Routes

This page summarizes route-level documentation for subscription, redemption code, and order management features.

## Test Environment

- Backend API example: `http://localhost:8005`
- Frontend example: `http://localhost:3000`
- Recommended Apifox variables:
  - `{{baseUrl}}`
  - `{{userJwt}}`
  - `{{userId}}`
  - `{{adminJwt}}`

## Notes

Keep route documentation aligned with backend behavior and update it whenever request or response contracts change.
