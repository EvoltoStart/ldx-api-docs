---
title: "LDX API 文档测试"
description: "用于验证 GitHub 文档仓库和 Mintlify 部署链路。"
---

# LDX API 文档测试

这个页面用于验证 `ldx-api-docs` 仓库可以被正常写入，并可作为 Mintlify 文档站点的入口。

如果你能在 Mintlify 预览或线上站点看到这个页面，说明基础部署链路已经打通。

## 后续计划

- 接入 `docs/openapi/api.json` 生成业务 API Reference。
- 接入 `docs/openapi/relay.json` 生成模型中转 API Reference。
- 从内网 GitLab CI 同步白名单文档到这个仓库。
